#include<stdio.h>
void swap(int *x, int *y)
{int temp=*x;
 *x = *y;
 *y = temp;
}
void sel_sort(int array[], int size)
{int i,j,min;
 for(i=0;i<(size-1);i++)
 {
   min=i;
   for(j=i+1;j<n;j++)
   {
    if(array[j]<array[min])
    {min=j}
   }
  swap(&array[i],&array[min]); 
 }

  
}
