void sawp(int *x, int *y)
{
int temp= *x;
*x =  *y;
*y = temp;
}

void bubble_sort(int array[],n)
{int i,j;
  for(i=o;i<n-1;i++)
  { for(j=0;j<n-1-i;j++)
     {if(array[j]>array[j+1])
      {swap(&array[j],&array[j+1]);}
     
     }
  }
}
